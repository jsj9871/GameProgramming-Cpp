#include <iostream>	// 입출력 스트림
#include "Monster.h"

using namespace std;

int main()
{
	// 클래스 구현부
	/*
	Monster monster1;
	Monster monster2;

	monster1.Attack(10);
	*/

	// 영수증
	/*
	int sum = 0;
	int temp = 0;
	int thing = 0;

	int price = 0;
	int thingNumber = 0;

	cin >> sum;
	cin >> thing;

	for (int i = 0; i < thing; i++)
	{
		cin >> price;
		cin >> thingNumber;

		temp += price * thingNumber;
	}

	if (sum == temp)
	{
		cout << "Yes";
	}
	else
	{
		cout << "No";
	}
	*/

	return 0;
}